# Existing VM — pve-node-01, VMID 100
# Provisioned manually. This file documents the config for re-creation.
profile: dev-base
app: ragflow

vm:
  id: 100
  name: ragflow
  node: pve-node-01

# Hardware override: dev-base profile is 8192MB but ragflow needs more headroom
hardware:
  memory: 10000
  cores: 8
  disk: 32G

network:
  ip: 192.168.1.100
  gateway: 192.168.1.1
  netmask: 24
  dns: 192.168.1.1
  searchdomain: home
