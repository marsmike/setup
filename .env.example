# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.
#
# All setup scripts source .env automatically if it lives next to them.

# ── Identity ────────────────────────────────────────────────────────────────

# Username created on servers and used throughout setup
SETUP_USER=mike

# Default remote host for 00_local_sshkey.sh  (user@host)
SETUP_HOST=mike@5.199.130.154

# SSH key path used/generated by 00_local_sshkey.sh
# SETUP_SSH_KEY=~/.ssh/id_ed25519

# ── Dotfiles ─────────────────────────────────────────────────────────────────

# GitHub username whose marsmike/dotfiles repo chezmoi will apply
DOTFILES_REPO=marsmike

# ── GitHub ───────────────────────────────────────────────────────────────────

# Personal Access Token — skips interactive gh auth login
# Needs scopes: repo, read:org, workflow, gist
# Generate at: https://github.com/settings/tokens
GH_TOKEN=
