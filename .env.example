# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.
#
# Setup scripts will source .env automatically if present.

# GitHub Personal Access Token — used by 02_dotfiles.sh to authenticate gh
# without interactive login. Needs scopes: repo, read:org, workflow, gist.
# Generate at: https://github.com/settings/tokens
GH_TOKEN=
